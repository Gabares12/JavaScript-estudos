const nome = ["ana Julina", "Caio vinicius", "BIA silva "];



// cada item do array é capturado dentro da variavel NOME
const nomesPadronizados = nomes.map((nome) => nome.toUpperCase());

console.log(nomesPadronizados);
